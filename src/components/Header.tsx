import React from "react";
import { AppBar, Toolbar, Typography, Box, Select, MenuItem } from "@mui/material";
import { UseDispatch } from "react-redux";
import {useSubject} from '../features/search/searchSlice';